# Email Triage RL Environment

This project is an OpenEnv-compliant environment for classifying emails into:
- Spam
- Important
- Urgent
- Normal

It uses a rule-based baseline policy and supports RL interaction via step/reset API.